services:
  scrum-poker:
    build: .
    ports:
      - "8000:8000"
    environment:
      BASE_PATH: "/poker"
      PORT: "8000"
      COUNTER_DB_PATH: "/data/counter.sqlite3"
    volumes:
      - counter-data:/data

volumes:
  counter-data:
